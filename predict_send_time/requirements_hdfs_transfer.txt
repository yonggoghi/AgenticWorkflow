# HDFS Transfer Script Requirements

# Core dependencies
python-dotenv>=0.19.0

# Parquet file handling (파티션 통합 기능 필요 시)
pyarrow>=10.0.0

# Optional: 더 나은 성능을 위해
# pandas>=1.3.0  # PyArrow와 함께 사용 가능

# Note: hdfs dfs 명령어는 시스템에 Hadoop이 설치되어 있어야 합니다.
# Note: sshpass는 스크립트가 자동으로 설치 시도합니다.
